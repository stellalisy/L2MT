name: l2mt
channels:
  - defaults
  - pytorch
  - conda-forge
dependencies:
  - python=3.9.*
  - pip
  - numpy=1.19.*
  - pytorch=1.9.*
  - scipy=1.5.*
  - tqdm
  - pip:
      - more-itertools
      - torchtyping
      - transformers
      - datasets
      - fastBPE
      - sentencepiece
      - sacrebleu
      - sacremoses
      - scikit-learn
      - matplotlib